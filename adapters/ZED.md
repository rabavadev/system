# Zed Agent Adapter

Use the root `AGENTS.md` as the master policy.

Before coding:
1. inspect repository evidence,
2. read `PROJECT_RULES.md` and `PROJECT_STATE.md`,
3. route through `skills/using-skills/SKILL.md`,
4. load only relevant skills.

Do not create parallel V2/new/final implementations.
