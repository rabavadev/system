# server/tools

Tools exposed to agents. Each tool is a small typed unit with a name, schema, and handler.

Server-only code. Never import from client bundles (`src/components`,
`src/routes`, `src/features` UI files).
