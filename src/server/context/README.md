# server/context

Request and workspace context assembly for server functions: current workspace, brand, permissions.

Server-only code. Never import from client bundles (`src/components`,
`src/routes`, `src/features` UI files).
